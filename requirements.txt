python-telegram-bot==20.7
python-dotenv>=1.0.0

Если нужно установить зависимости, выполните:
```
py -3.12 -m pip install -r requirements.txt
```

Если потребуется добавить другие библиотеки — дайте знать! 